## 初始化
- 接口说明

```
boolean initFunSDKPush(Context context,SMCInitInfo info, int pushType);
```
- 参数说明

| 参数      |类型 | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| context|Context| 上下文| 非空|
| info|SMCInitInfo|推送初始化信息 | 非空|
| pushType|int| 推送类型   | 比如：PUSH_TYPE_XM |

##### pushType 推送类型
| 字段 | 说明|
| ------ | ------ |
| PUSH_TYPE_XM | 雄迈推送 |
| PUSH_TYPE_GOOGLE| Google推送|
| PUSH_TYPE_HUAWEI| 华为推送 |
| PUSH_TYPE_XG| 信鸽推送|
| PUSH_TYPE_WECHAT| 微信推送|
| PUSH_TYPE_LINE| Line推送|
| PUSH_TYPE_XIAOMI| 小米推送|
| PUSH_TYPE_VIVO| VIVO推送|
| PUSH_TYPE_OPPO| OPPO推送|
| PUSH_TYPE_HONOR| 荣耀推送|

- 示例代码

```
XMPushManager xmPushManager = new XMPushManager(xmPushLinkResult);
String pushToken = XUtils.getPushToken(this);
if (!StringUtils.isStringNULL(pushToken)) {
    SMCInitInfo info = new SMCInitInfo();
    G.SetValue(info.st_0_user, XMAccountManager.getInstance().getAccountName());
    G.SetValue(info.st_1_password, XMAccountManager.getInstance().getPassword());
    G.SetValue(info.st_2_token, pushToken);
    xmPushManager.initFunSDKPush(info,PUSH_TYPE_XM);
}

```
- URL初始化报警消息示例代码

```
XMPushManager  xmPushManager = new XMPushManager(xmPushLinkResult);
String pushToken = XUtils.getPushToken(this);//推送订阅Token，自定义：能确保每台手机唯一性
if (!StringUtils.isStringNULL(pushToken)) {
    SMCInitInfo info = new SMCInitInfo();
    G.SetValue(info.st_0_user, XMAccountManager.getInstance().getAccountName());//云账号
    G.SetValue(info.st_1_password, XMAccountManager.getInstance().getPassword());//云密码
    G.SetValue(info.st_2_token, pushToken);//推送订阅Token，自定义 能确保每台手机唯一性
    G.SetValue(info.st_5_appType,"Third:http://xxxxx xxxx");
    //(例：Third:http(s)://xmey.net:80/xxx/xxx 或 Third:ip:80 中间用“:”做分隔符)
    xmPushManager.initFunSDKPush(this,info,PUSH_TYPE_XM);
}

```
> [服务器端报警消息对接说明](https://developer.jftech.com/docs/?menusId=ab9a6dddd50c46a6af8a913b472ed015&siderid=205e3f2a9a47424ea739f5f5b679eee6&lang=zh)

## 订阅
- 接口说明

```
void linkAlarm(String devId, int seq);
```
- 参数说明

| 参数  | 类型     | 描述    | 取值范围|
| :--------- | -------- |------- |------- |
| devId|String| 设备序列号|非空|
| seq|int|回传Id |默认值：0|

- 示例代码

```
xmPushManager.linkAlarm(devId,0);
```
## 取消订阅
- 接口说明

```
void unLinkAlarm(String devId, int seq);
```
- 参数说明

| 参数    |类型   | 描述     | 取值范围|
| :--------- | -------- |--- |--- |
| devId|String| 设备序列号| 非空|
| seq|int|回传Id |默认值：0|

- 示例代码

```
xmPushManager.unLinkAlarm(devId,0);
```

## 接收推送信息

```
public interface OnXMPushLinkListener {
        /**
         * 订阅初始化
         *
         * @param pushType
         * @param errorId
         */
        void onPushInit(int pushType, int errorId);

        /**
         * 订阅回调
         *
         * @param pushType
         * @param devId
         * @param seq
         * @param errorId
         */
        void onPushLink(int pushType, String devId, int seq, int errorId);

        /**
         * 取消订阅回调
         *
         * @param pushType
         * @param devId
         * @param seq
         * @param errorId
         */
        void onPushUnLink(int pushType, String devId, int seq, int errorId);

        /**
         * 从服务端获取订阅状态
         *
         * @param pushType 推送类型
         * @param devId    设备序列号
         * @param isLinked 是否订阅
         */
        void onIsPushLinkedFromServer(int pushType, String devId, boolean isLinked);

        /**
         * 报警推送回调
         *
         * @param pushType
         * @param devId
         * @param msg
         * @param ex
         */
        void onAlarmInfo(int pushType, String devId, Message msg, MsgContent ex);

        /**
         * 订阅断开回调
         *
         * @param pushType
         * @param devId
         */
        void onLinkDisconnect(int pushType, String devId);

        /**
         * 微信推送状态
         *
         * @param devId
         * @param state
         * @param errorId
         */
        void onWeChatState(String devId, int state, int errorId);

        /**
         * 第三方推送状态
         *
         * @param info
         * @param pushType
         * @param state
         * @param errorId
         */
        void onThirdPushState(String info, int pushType, int state, int errorId);

        /**
         * 所有订阅取消回调
         */
        void onAllUnLinkResult();

        /**
         * FunSDK返回的所有数据
         *
         * @param msg
         * @param ex
         * @return
         */
        void onFunSDKResult(Message msg, MsgContent ex);

    }
```

![](https://obs-xm-pub.obs.cn-south-1.myhuaweicloud.com/docs/20231215/1702633502437.gif)