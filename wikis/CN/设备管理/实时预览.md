## 接口总览
| 接口类 | 接口名称| 接口描述 | 备注 |
| - | - | - | - |
| DeviceManager| createMonitorPlayer  | 创建播放器| |
| MonitorManager| startMonitor| 开始实时预览| |
| MonitorManager| pausePlay| 暂停实时预览| |
| MonitorManager| rePlay| 恢复实时预览| |
| MonitorManager| stopPlay| 停止实时预览| |
| MonitorManager| destroyPlay|  销毁播放器| |
| MonitorManager| setStreamType| 切换码流| |
| MonitorManager| setShape| 设置视频画面形状(球状、碗状)|仅针对鱼眼设备 |
| MonitorManager| setMount| 设置画面放置(墙壁 、桌面)| 仅针对鱼眼设备|
| MonitorManager| setDoorBellWallMode| 设置是否为门铃墙壁模式| 仅针对鱼眼设备|
| MonitorManager| capture| 实时抓图| |
| MonitorManager| startRecord|开始实时剪切| |
| MonitorManager| stopRecord|停止实时剪切| |
| MonitorManager| openVoiceBySound|开启伴音| |
| MonitorManager| closeVoiceBySound|停止伴音| |  
| MonitorManager| startTalkByHalfDuplex|开启半双工对讲| |  
| MonitorManager| stopTalkByHalfDuplex|关闭半双工对讲| |  
| MonitorManager| startTalkByDoubleDirection|开启全双工对讲| |  
| MonitorManager| stopTalkByDoubleDirection|关闭全双工对讲| |  
| MonitorManager| setVideoFlip|设置图像翻转| |  
| MonitorManager| setVideoFullScreen|设置是否满屏显示| |  
| MonitorManager| setHardDecode|设置是否硬解码| |  

### 1.创建播放器
- 接口说明

```
MonitorManager createMonitorPlayer(ViewGroup playView, String devId);
```
- 参数说明

| 参数      |类型 | 描述     | 取值范围|
| :--------- | -------- |------ |------ |
| playView  | ViewGroup | 播放画布   | 非空|
| devId  |String | 设备序列号 | 非空|

- 示例代码

```
DeviceManager.getInstance().createMonitorPlayer(surfaceView,devId);
```

### 2.开始实时预览
- 接口说明

```
void startMonitor();
```


- 示例代码

```
MonitorManager mediaManager = createMonitorPlayer(viewGroup, devId);
mediaManager.startMonitor();//打开视频

```
### 3.暂停实时预览
- 接口说明

```
void pausePlay();
```

- 示例代码

```
mediaManager.pausePlay();//暂停播放

```
### 4.恢复实时预览
- 接口说明

```
void rePlay();
```

- 示例代码

```
mediaManager.rePlay();
```

### 5.停止实时预览
- 接口说明

```
void stopPlay();
```

- 示例代码

```
mediaManager.stopPlay();
```

### 6.销毁播放器
- 接口说明

```
void destroyPlay();
```

- 示例代码

```
mediaManager.destroyPlay();
```

### 7.切换码流
- 接口说明

```
T setStreamType(int streamType);
```
- 参数说明

| 参数      |类型 | 描述     | 取值范围|
| :--------- | -------- |------ |------ |
| streamType   | int | 码流类型   | 0:主码流 <br> 1:辅码流|

- 示例代码

```
mediaManager.setStreamType(SDKCONST.StreamType.Extra);
```
### 8.设置视频画面形状(球状、碗状)

- 接口说明

```
void setShape(int shape);
```
- 参数说明

| 参数    | 类型   | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| shape   | int | 画面形状: 球状、碗状| 参考[XMVRShape]|
- 示例代码

```
mediaManager.setShape(XMVRShape.Shape_Ball);
```

### 9.设置画面放置(墙壁 、桌面)
- 接口说明

```
void setMount(int mount);
```
- 参数说明

| 参数  |   类型   | 描述    | 取值范围|
| :--------- | -------- |------ |------ |
| mount | int | 画面放置   | 参考[XMVRMount]|

- 示例代码

```
mediaManager.setMount(XMVRMount.Ceiling);
```

### 10.设置是否为门铃墙壁模式
- 接口说明

```
void setDoorBellWallMode(boolean isDoorBellWallMode);
```
- 参数说明

| 参数  |类型     | 描述     | 取值范围|
| :--------- | -------- |--- |--- |
| isDoorBellWallMode|boolean| 是否为门铃墙壁模式   | true:设置为墙壁模式<br> false:设置为普通模式|

- 示例代码

```
mediaManager.setDoorBellWallMode(true);
```


### 11.实时抓图
- 接口说明

```
String capture(String dir);
```
- 参数说明

| 参数   | 类型    | 描述     | 取值范围|
| :--------- | -------- |---- |---- |
| dir | String  | 保存图片的文件夹路径  | 非空|

- 示例代码

```
String imgPath = mRecordPlayer.capture(MyApplication.PATH_PHOTO);
```

### 12.开始实时剪切
- 接口说明

```
boolean startRecord(String dir);
```
- 参数说明

| 参数   | 类型    | 描述     | 取值范围|
| :--------- | -------- |------ |------ |
| dir|String| 保存视频的文件夹路径   | 非空|

- 示例代码

```
mediaManager.startRecord(SDKDemoApplication.PATH_VIDEO);
```
### 13.停止实时剪切

- 接口说明

```
String stopRecord();
```

- 示例代码

```
mediaManager.stopRecord();
```

### 14.开启伴音

- 接口说明

```
void openVoiceBySound();
```

- 示例代码

```
mediaManager.openVoiceBySound();
```

### 15.停止伴音
- 接口说明

```
void closeVoiceBySound();
```

- 示例代码

```
mediaManager.closeVoiceBySound();
```

### 16.开启半双工对讲

- 接口说明

```
void startTalkByHalfDuplex(Context context);
```
- 参数说明

| 参数 |类型      | 描述    | 取值范围|
| :--------- | -------- |---- |---- |
| context|Context | 上下文   | 非空|

- 示例代码

```
mediaManager.startTalkByHalfDuplex(context);
```

### 17.关闭半双工对讲
- 接口说明

```
void stopTalkByHalfDuplex();
```

- 示例代码

```
mediaManager.stopTalkByHalfDuplex();
```

### 18.开启全双工对讲

- 接口说明

```
void startTalkByDoubleDirection(Context context, boolean uploadTalk);
```
- 参数说明

| 参数    |类型   | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| context|Context   | 上下文  | 非空|
| uploadTalk|boolean   | 是否要上传对讲语音 | true:将手机采集的音频上传给设备 <br> false:不将手机采集的音频上传给设备|

- 示例代码

```
mediaManager.startTalkByDoubleDirection(context,true);//开启双向对讲
```

### 19.关闭全双工对讲
- 接口说明

```
void stopTalkByDoubleDirection();
```

- 示例代码

```
mediaManager.stopTalkByDoubleDirection();
```

### 20.设置图像翻转
- 接口说明

```
/**
 * 设置图像翻转
 *
 * @param flipType 翻转类型：0 不翻转 1 翻转90度 2 翻转180度 3 翻转270度
 */
 void setVideoFlip(int flipType);
```

- 参数说明

| 参数    |类型   | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| flipType|int | 设置图像翻转  | 默认0不翻转 翻转类型：0 不翻转 1 翻转90度 2 翻转180度 3 翻转270度|

- 示例代码

```
mediaManager.setVideoFlip(1);//翻转90度
```
### 21.设置是否满屏显示
- 接口说明

```
/**
 * 设置是否满屏显示
 *
 * @param fullScreen 是否满屏显示
 */
 void setVideoFullScreen(boolean fullScreen);
```
- 参数说明

| 参数    |类型   | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| fullScreen|boolean | 是否满屏显示  | 默认false 按照视频比例显示|

- 示例代码

```
mediaManager.setVideoFullScreen(true);
```

### 22.设置是否开启硬解码
- 接口说明

```
/**
 * 设置是否开启硬解码，默认关闭
 *
 * @param enable 是否开启硬解码，默认关闭
 */
 void setHardDecode(boolean enable);
```
- 参数说明

| 参数    |类型   | 描述    | 取值范围|
| :--------- | -------- |----- |----- |
| enable|boolean | 是否开启硬解码  | 默认false 软解码,true 硬解码|

- 示例代码

```
mediaManager.setHardDecode(true);
```

#### XMVRShape 视频形状
| 字段名 | 说明|
| ------ | ------ |
| Shape_Ball：0| 球/半球(360VR默认) |
| Shape_Ball_Hat：1| 球/半球,帽子型,半球倒转|
| Shape_Ball_Bowl：2| 球/半球,碗状和Ball_Hat相反 |
| Shape_Cylinder：3| 圆柱|
| Shape_CylinderS：4| 圆柱(近距离型,只能看到部分)|
| Shape_Rectangle：5| 矩形,拉伸展开|
| Shape_Rectangle_2R：6| 矩形,两行的模式|
| Shape_Grid_4R：7| 球/半球矫正,4宫格/画面模式,初始在4个不同的角度|
| Shape_Grid_1O_5R：8| 左上角圆图显示,右边和下面5个小窗口显示|
| Shape_Grid_1L_2R：9| 上面两个圆放大的矫正效果,下面是一行的效果|
| Shape_Grid_3R：10|3宫格/画面|

#### XMVRMount 画面放置
| 字段名 | 说明|
| ------ | ------ |
| Ceiling：0| 天花板模式 |
| Wall：1| 墙壁模式|
| WallInverted：2| 墙壁反转 |
| Table：3| 墙壁反转|