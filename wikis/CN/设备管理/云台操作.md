
> 建议：摇头速度可以设置成3档，然后水平转动的步长是垂直的步长2倍。
比如： 
慢：水平步长是2 垂直步长是1
正常：水平步长是4 垂直步长是2
快：水平步长是8 垂直步长是4

- 接口说明

```
/**
 * 云台控制
 *
 * @param ptzCtrlInfoBean 云台控制参数
 * @param listener        回调处理
 * @return
 */
boolean devPTZControl(PtzCtrlInfoBean ptzCtrlInfoBean, DeviceManager.OnDevManagerListener listener);
```
- 示例代码

```
 /**
 * 设备云台控制
 * Equipment head control
 * @param chnId       通道号
 * @param nPTZCommand 云台命令
 * @param speed       运动步长 *默认值4，范围1 ~ 8, 设备程序会把该步长转换为相应的速度值，1的速度最慢，8的速度最快。
                      推荐水平三档：2、4、8 垂直三档：1、2、4
 * @param bStop       是否停止
 */
@Override
public void devicePTZControl(int chnId, int nPTZCommand, int speed, boolean bStop) {//Cloud console, operation button callback
    if (!monitorManagers.containsKey(chnId)) {
        return;
    }

    MonitorManager mediaManager = monitorManagers.get(chnId);
    if (mediaManager != null) {
        PtzCtrlInfoBean ptzCtrlInfoBean = new PtzCtrlInfoBean();
        ptzCtrlInfoBean.setDevId(mediaManager.getDevId());
        ptzCtrlInfoBean.setPtzCommandId(nPTZCommand);
        ptzCtrlInfoBean.setStop(bStop);//是否停止云台转动，如果是false的话，云台会一直转动下去
        ptzCtrlInfoBean.setChnId(chnId);//通道号
        ptzCtrlInfoBean.setSpeed(speed);//云台操作速度（步长）
        manager.devPTZControl(ptzCtrlInfoBean, null);
    }
}
```

![](https://obs-xm-pub.obs.cn-south-1.myhuaweicloud.com/docs/20231215/1702627860527.gif)