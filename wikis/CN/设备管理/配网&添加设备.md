## 添加设备之前先要登录账号
### 1. 快速配置添加设备
#### 1.1. 开始快速配网：
- 接口说明

```
void startQuickSetWiFi(String ssid, String pwd, String capabilities, DhcpInfo dhcpInfo, DeviceManager.OnQuickSetWiFiListener listener);
```
- 参数说明

| 参数     | 类型  | 描述     |取值范围 |
| :------|--- | -------- | --- |
| ssid      | String  | WiFi热点名   | 非空|
| pwd        | String   |WiFi密码 | 不能传null，但可以传""空字符串 |
| capabilities | String  | WiFi加密类型,从ScanResult中获取  | 非必传 |
| dhcpInfo   | DhcpInfo  | WiFi dhcp信息  | 非空|
| listener   | OnQuickSetWiFiListener  | 回调   | 非空|

- 示例代码

```
DeviceManager.getInstance().startQuickSetWiFi(wifiInfo, scanResult, dhcpInfo, pwd, new DeviceManager.OnQuickSetWiFiListener() {
    @Override
    public void onQuickSetResult(XMDevInfo xmDevInfo, int i) {
        AccountManager.getInstance().addDev(xmDevInfo, isUnbindDevUnderOther, new BaseAccountManager.OnAccountManagerListener() {
            @Override
            public void onSuccess(int msgId) {
                Toast.makeText(iDevQuickConnectView.getContext(),
                        "添加成功",Toast.LENGTH_LONG).show();
                iDevQuickConnectView.onAddDevResult();
            }

            @Override
            public void onFailed(int msgId, int errorId) {
                Toast.makeText(iDevQuickConnectView.getContext(),
                        "添加失败:" + errorId,Toast.LENGTH_LONG).show();
                iDevQuickConnectView.onAddDevResult();
            }

            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {

            }
        });
    }
});
```
#### 1.2. 停止快速配网：
- 接口说明

```
void stopQuickSetWiFi();
```
<hr style="height:1px;border:none;border-top:1px solid #eeeeee;"/>

### 2. 手动添加设备

- 接口说明

```
void addDev(XMDevInfo xmDevInfo, boolean isUnbindDevUnderOther, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数     | 类型  | 描述      |取值范围 |
| :------|--- | -------- | ---- |
| xmDevInfo   | XMDevInfo | 设备信息,参考[XMDevInfo](https://docs.jftech.com/docs?menusId=ab0ed73834f54368be3e375075e27fb2&siderid=31a4477965ea45a5ae6af7770b28f161&lang=zh#docs-hash-5)   | 非空|
| isUnbindDevUnderOther   | boolean |是否要从其他账号下解绑该设备 | 默认false|
| listener   | OnAccountManagerListener | 回调   | 非空|

- 示例代码

```
SDBDeviceInfo deviceInfo = new SDBDeviceInfo();
G.SetValue(deviceInfo.st_0_Devmac,devId);
G.SetValue(deviceInfo.st_5_loginPsw,pwd);
G.SetValue(deviceInfo.st_4_loginName,userName);
XMDevInfo xmDevInfo = new XMDevInfo();
xmDevInfo.sdbDevInfoToXMDevInfo(deviceInfo);
AccountManager.getInstance().addDev(xmDevInfo, new BaseAccountManager.OnAccountManagerListener() {
    @Override
    public void onSuccess(int i) {
        if (iDevSnConnectView != null) {
            iDevSnConnectView.onAddDevResult(true);
        }
    }

    @Override
    public void onFailed(int i, int i1) {
        if (iDevSnConnectView != null) {
            iDevSnConnectView.onAddDevResult(false);
        }
    }

    @Override
    public void onFunSDKResult(Message message, MsgContent msgContent) {

    }
});
```

<hr style="height:1px;border:none;border-top:1px solid #eeeeee;" />

### 3. 二维码配网添加
#### 3.1. 获取二维码配网的WiFi二维码信息:

- 接口说明

```
Bitmap initDevToRouterByQrCode(String ssid, String wifiPwd, int pwdType, String macAddress, String ipAddress, DeviceManager.OnDevWiFiSetListener listener);

```
- 参数说明

| 参数     | 类型    | 描述     | 取值范围| 
| :-----|---- | -------- |---|
| ssid| String| WiFi热点名称   | 非空 |
| wifiPwd | String  | WiFi密码 | 不能传null，但可以传""空字符串|
| pwdType   | int    | 加密类型   | 0：没有WiFi密码 1：有WiFi密码|
| macAddress | String   | 手机的mac地址   | 默认值："020000000000"| 
| ipAddress    | String    | 手机连接的IP地址     | 不能传null，但可以传"" | 
| listener     | OnDevWiFiSetListener | 回调   |非空 | 

- 示例代码

```
WifiInfo wifiInfo = xmWifiManager.getWifiInfo();
DhcpInfo dhcpInfo = xmWifiManager.getDhcpInfo();//获取Dhcp信息
String ssid = getConnectWiFiSsid();//获取当前连接的WiFi热点
scanResult = xmWifiManager.getCurScanResult(ssid);//通过ssid获得的ScanResult对象包含更多网络信息对象
int pwdType = XUtils.getEncrypPasswordType(scanResult.capabilities);//获取WiFi密码加密类型
if (pwdType == 3 && (wifiPwd.length() == 10 || wifiPwd.length() == 26)) {
    wifiPwd = XUtils.asciiToString(wifiPwd);
}

String ipAddress = Formatter.formatIpAddress(dhcpInfo.ipAddress);//获取IP地址
String macAddress = XMWifiManager.getWiFiMacAddress().replace(":", "");//获取MAC地址

//将以上信息生成二维码
Bitmap bitmap = manager.initDevToRouterByQrCode(ssid, wifiPwd, pwdType, macAddress, ipAddress, new DeviceManager.OnDevWiFiSetListener() {
    @Override
    public void onDevWiFiSetState(int errorId) {
        if (errorId < 0) {
          //TODO 如果失败了，可以尝试多次
        }
    }

    @Override
    public void onDevWiFiSetResult(XMDevInfo xmDevInfo) {
        //配网成功后，调用添加设备接口
        XMAccountManager.getInstance().addDev(xmDevInfo, new BaseAccountManager.OnAccountManagerListener() {
            @Override
            public void onSuccess(int msgId) {
               
            }

            @Override
            public void onFailed(int msgId, int errorId) {
                
            }

            @Override
            public void onFunSDKResult(Message msg, MsgContent ex) {

            }
        });
    }
});

```
<hr style="height:1px;border:none;border-top:1px solid #eeeeee;"/>

#### 3.2. 开始二维码配网:
- 接口说明

```
void startDevToRouterByQrCode();

```

- 示例代码

```
//将二维码对准设备的镜头，开始配网
DeviceManager.getInstance().startDevToRouterByQrCode();
```

#### 3.3. 停止二维码配网:
- 接口说明

```
void stopSetDevToRouterByQrCode();
```


- 示例代码

```
DeviceManager.getInstance().stopSetDevToRouterByQrCode();
```

#### 3.4. 释放资源:

- 接口说明

```
void unInitDevToRouterByQrCode();
```

- 示例代码

```
DeviceManager.getInstance().unInitDevToRouterByQrCode();
```

### 4. 局域网添加设备
#### 4.1.获取局域网设备列表
- 接口说明

```
void searchLanDevice(OnSearchLocalDevListener listener);
```
- 参数说明

| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| listener |  OnSearchLocalDevListener   | 回调     | 非空|

- 示例代码

```
DeviceManager.getInstance().searchLanDevice(new DeviceManager.OnSearchLocalDevListener() {
            /**
             * 
             * @param list 搜索到的局域网设备列表
             */
            @Override
            public void onSearchLocalDevResult(List<XMDevInfo> list) {
                
            }
        });
```

#### 4.2.添加设备
- 接口说明

```
void addDev(XMDevInfo xmDevInfo, BaseAccountManager.OnAccountManagerListener listener )
```
- 参数说明

| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| xmDevInfo|  XMDevInfo | 设备信息,参考[XMDevInfo](https://docs.jftech.com/docs?menusId=ab0ed73834f54368be3e375075e27fb2&siderid=31a4477965ea45a5ae6af7770b28f161&lang=zh#docs-hash-5)     | 非空|
| listener |  OnAccountManagerListener | 回调     | 非空|

- 示例代码

```
XMAccountManager.getInstance().addDev(xmDevInfo, new BaseAccountManager.OnAccountManagerListener() {
                    @Override
                    public void onSuccess(int msgId) {
                        if (msgId == EUIMSG.SYS_ADD_DEVICE) {
                            if (iDevLanConnectView != null) {
                                iDevLanConnectView.onAddDevResult(true,0);
                       }
                    }

                    @Override
                    public void onFailed(int msgId, int errorId) {
                        if (msgId == EUIMSG.SYS_ADD_DEVICE) {
                            if (iDevLanConnectView != null) {
                                 iDevLanConnectView.onAddDevResult(false,errorId);
                              }         
                       }
                    }

                    @Override
                    public void onFunSDKResult(Message msg, MsgContent ex) {

                    }
                });
```

### 5. 蓝牙配网
#### 5.1. 搜索蓝牙设备
- 接口说明

```
void startScan(Context context, final IXMBleManagerListener iBleManagerListener)
```
- 参数说明


| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| listener |  IXMBleManagerListener | 回调     | 非空|

- 示例代码

```
XMBleManager.getInstance().setTimeOut((int) (SEARCH_TIME * 1000)).startScan(AddDeviceWithWifiBaseStationActivity.this, new IXMBleManagerListener()
                {
                    @Override
                    public void onBleScanResult(XMBleInfo xmBleInfo) {
                        super.onBleScanResult(xmBleInfo);
                       
                    }
                });
```

#### 5.2. 停止蓝牙设备
- 接口说明

```
void stopScan()
```

- 示例代码

```
XMBleManager.getInstance().stopScan()
```

#### 5.3. 连接蓝牙设备
- 接口说明

```
void connect(String mac, IXMBleManagerListener iBleManagerListener);
```
- 参数说明

| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| mac| String | 蓝牙设备Mac地址    | 非空 | 
| listener |  IXMBleManagerListener | 回调     | 非空|

- 示例代码

```
XMBleManager.getInstance().connect("mac地址", IXMBleManagerListener);
```

#### 5.4. 断开蓝牙连接
- 接口说明

```
void disConnect(String mac);
```
- 参数说明

| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| mac| String | 蓝牙设备Mac地址    | 非空 | 

- 示例代码

```
XMBleManager.getInstance().disConnect("mac地址");
```

#### 5.5. 通过蓝牙方式配网
- 接口说明

```
void connectWiFi(String mac, String ssid, String pwd, String capabilities, IXMBleManagerListener iBleManagerListener);
```
- 参数说明

| 参数    | 类型   | 描述      | 取值范围| 
| :------|--- | -------- | --- |
| mac| String | 蓝牙设备Mac地址    | 非空 | 
| ssid| String | WiFi热点名称    | 非空 | 
| pwd        | String   |WiFi密码 | 不能传null，但可以传""空字符串 |
| capabilities | String  | WiFi加密类型,从ScanResult中获取  | 非必传 |
| iBleManagerListener| IXMBleManagerListener | 回调    | 非空 | 

- 示例代码

```
XMBleManager.getInstance().connectWiFi(mac, ssid, pwd, capabilities, iBleManagerListener);
```

##### XMDevInfo
| 字段名 | 说明 |
| ------ | ------ |
| devId | 设备序列号|
| devName| 设备名称|
| devUserName| 设备登录名|
| devPassword| 设备登录密码|
| devIp| 设备IP地址|
| pid| 产品类型，和devType差不多，新产品中会替代devType|
| mac| MAC地址|
| devPort| 设备端口号|
| devType| 设备类型|
| devState| 设备在线状态|
| cloudState| 云服务状态|
| cloudExpired| 云服务过期状态|
| systemInfoBean| 缓存的设备信息|
| systemInfoExBean| 缓存的设备扩展信息|
| systemFunctionBean| 缓存的设备能力集|
| sdbDevInfo| 设备在线状态|
| cloudState| 云服务状态|
| cloudExpired| 云服务过期状态|
| systemInfoBean| 兼容老的SDK中的设备信息|
| chipOEMId| 芯片OEMId|


![](https://obs-xm-pub.obs.cn-south-1.myhuaweicloud.com/docs/20231215/1702631198711.gif)

