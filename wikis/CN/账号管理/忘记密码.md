
## 接口总览
| 接口类 | 接口名称| 接口描述 | 备注 |
| - | - | - | - |
| XMAccountManager| sendEmailCodeForResetPwd | 发送邮箱重置密码验证| |
| XMAccountManager| sendPhoneCodeForResetPwd| 发送手机重置密码验证| |
| XMAccountManager| verifyEmailCode| 验证邮箱验证码| |
| XMAccountManager| verifyPhoneCode| 验证手机号验证码| |
| XMAccountManager| resetPwdByEmail| 通过邮箱方式重置密码| |
| XMAccountManager| resetPwdByPhone| 通过手机号方式重置密码| |

### 1. 获取验证码
#### 1.1. 通过邮箱获取验证码

- 接口说明

```
/**
 * 发送邮箱重置密码验证
 *
 * @param email 邮箱
 * @param ls    回调
 * @return
 */
boolean sendEmailCodeForResetPwd(String email, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| email   |String| 邮箱| |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().sendEmailCodeForResetPwd(email, new BaseAccountManager.OnAccountManagerListener() {
            @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
```

#### 1.2. 通过手机号码获取验证码
- 接口说明

```
/**
 * 发送手机重置密码验证
 *
 * @param phone 手机号
 * @param ls 回调
 * @return
 */
boolean sendPhoneCodeForResetPwd(String phone, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| phone | String     | 手机号   |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().sendPhoneCodeForResetPwd(phone, new BaseAccountManager.OnAccountManagerListener() {
           @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
        });
```
### 2. 提交验证码（验证验证码）
#### 2.1. 验证邮箱验证码

- 接口说明

```
/**
 * 验证邮箱验证码
 *
 * @param email      邮箱
 * @param verifyCode 验证码
 * @param ls         回调
 * @return
 */
boolean verifyEmailCode(String email, String verifyCode, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| email   | String  | 邮箱   |
| verifyCode | String | 验证码   |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().verifyEmailCode(email, verifyCode, new BaseAccountManager.OnAccountManagerListener({
            @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
        });
```
#### 2.2. 验证手机验证码

- 接口说明

```
/**
 * 验证手机号验证码
 *
 * @param phone      手机号
 * @param verifyCode 验证码
 * @param ls         回调
 * @return
 */
boolean verifyPhoneCode(String phone, String verifyCode, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| phone      | String  | 手机号   |
| verifyCode | String | 验证码   |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().verifyPhoneCode(phone, verifyCode, new BaseAccountManager.OnAccountManagerListener({
            @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
        });
```

### 3. 修改密码
#### 3.1.通过邮箱修改密码
- 接口说明

```
/**
 * 通过邮箱方式重置密码
 *
 * @param email    邮箱
 * @param newPassw 新密码
 * @param ls       回调
 * @return
 */
boolean resetPwdByEmail(String email, String newPassw, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| email| String  | 邮箱      |
| newPassw| String | 新密码   |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().resetPwdByEmail(email, newPwd, new BaseAccountManager.OnAccountManagerListener() {
            @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
        });
```

#### 3.2.通过手机号码修改密码
- 接口说明

```
/**
 * 通过手机号方式重置密码
 *
 * @param phone    手机号
 * @param newPassw 新密码
 * @param ls       回调
 * @return
 */
boolean resetPwdByPhone(String phone, String newPassw, BaseAccountManager.OnAccountManagerListener ls);
```
- 参数说明

| 参数 | 类型| 描述|备注|
| - | - | - |-|
| phone| String  | 手机号     |
| newPassw| String | 新密码   |
| ls| OnAccountManagerListener |回调接口| 返回成功和失败，包括失败的错误信息|

- 示例代码

```
XMAccountManager.getInstance().resetPwdByPhone(phone, newPwd, new BaseAccountManager.OnAccountManagerListener() {
            @Override
            public void onSuccess(int msgId) {
                //成功回调
            }
        
            @Override
            public void onFailed(int msgId, int errorId) {
              //失败回调，errorId错误码
            }
        
            @Override
            public void onFunSDKResult(Message message, MsgContent msgContent) {
        
            }
        });
```
## 错误码
[查看错误码](https://docs.jftech.com/docs?menusId=ab0ed73834f54368be3e375075e27fb2&siderid=a5e54a6f9ab34703b0b53131f7a2b458&lang=zh&directory=false)